import type { ListOptions } from '../models/interfaces/list-options.interface';

const optionsArray: ListOptions[] = [];
const textAreaValue: string[] = [];
const parsedValue: string[][] = [];
const objectsArray: ListOptions[] = [];
export { optionsArray, textAreaValue, parsedValue, objectsArray };
